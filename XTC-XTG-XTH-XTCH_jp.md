本ドキュメントはAIによって生成されたものであり、細部に問題がある可能性があります。

# XTC/XTG/XTH/XTCH フォーマット技術仕様書

## ドキュメント情報

- **バージョン**: 1.0
- **日付**: 2025-01-XX
- **目的**: フォーマット変換のための公開技術仕様
- **対象プラットフォーム**: ESP32 電子ペーパーディスプレイデバイス

---

## フォーマット概要

ESP32電子ペーパーディスプレイ向けに設計された4つの独自ファイルフォーマット：

| フォーマット | 拡張子  | 用途                   | 説明                               |
|-------------|---------|------------------------|------------------------------------|
| **XTG**     | `.xtg`  | モノクロ画像            | 1ピクセルあたり1ビットのビットマップ  |
| **XTH**     | `.xth`  | 4階調グレースケール画像  | 1ピクセルあたり2ビットのビットマップ  |
| **XTC**     | `.xtc`  | コミックコンテナ         | 複数のXTGページを格納するコンテナ    |
| **XTCH**    | `.xtch` | コミックコンテナ（変種） | XTCフォーマットの変種               |

---

## バイトオーダー

**すべてのマルチバイト値はリトルエンディアン形式で格納されます。**

- リトルエンディアン: 最下位バイトが最下位アドレスに配置
- 例: `uint16_t 0x1234` → `[0x34, 0x12]`
- 例: `uint32_t 0x12345678` → `[0x78, 0x56, 0x34, 0x12]`

---

## XTGフォーマット（モノクロ画像）

### ファイル構造

XTGは電子ペーパーディスプレイ向けに最適化された、1ピクセルあたり1ビットのモノクロビットマップを格納します。

### ヘッダー（22バイト）

| オフセット | サイズ | 型        | フィールド   | 説明                          | 値                    |
|-----------|--------|----------|-------------|-------------------------------|----------------------|
| 0x00      | 4      | uint32_t | mark        | ファイル識別子                  | 0x00475458 ("XTG\0") |
| 0x04      | 2      | uint16_t | width       | 画像の幅（ピクセル）             | 1-65535              |
| 0x06      | 2      | uint16_t | height      | 画像の高さ（ピクセル）           | 1-65535              |
| 0x08      | 1      | uint8_t  | colorMode   | カラーモード                    | 0=モノクロ            |
| 0x09      | 1      | uint8_t  | compression | 圧縮方式                       | 0=非圧縮              |
| 0x0A      | 4      | uint32_t | dataSize    | 画像データサイズ（バイト）       | 計算値                |
| 0x0E      | 8      | uint64_t | md5         | MD5チェックサム（先頭8バイト）   | 任意                  |

### 画像データ

- **配置**: ヘッダーの直後（オフセット22バイト）
- **フォーマット**: ビットマップデータ、1ピクセルあたり1ビット
- **データサイズの計算式**: `dataSize = ((width + 7) / 8) * height`
- **ピクセルの格納方法**:
    - 行は上から下に格納
    - 各行は左から右に格納
    - 1バイトあたり8ピクセル（MSBファースト）
    - ビット順序: ビット7（MSB）= 最左ピクセル、ビット0（LSB）= 最右ピクセル

### ピクセル値のマッピング

| ビット値 | 表示色 |
|---------|-------|
| 0       | 黒    |
| 1       | 白    |

**注記**: 反転ビットマップ描画を使用する場合、ディスプレイはこれらの値を反転させることがあります（1=黒、0=白）。

---

## XTHフォーマット（4階調グレースケール画像）

### ファイル構造

XTHは4階調グレースケール電子ペーパーディスプレイ向けに、1ピクセルあたり2ビットのグレースケールビットマップを格納します。

### ヘッダー（22バイト）

XTGと同じ構造ですが、ファイル識別子が異なります：

| オフセット | サイズ | 型        | フィールド   | 説明                          | 値                    |
|-----------|--------|----------|-------------|-------------------------------|----------------------|
| 0x00      | 4      | uint32_t | mark        | ファイル識別子                  | 0x00485458 ("XTH\0") |
| 0x04      | 2      | uint16_t | width       | 画像の幅（ピクセル）             | 1-65535              |
| 0x06      | 2      | uint16_t | height      | 画像の高さ（ピクセル）           | 1-65535              |
| 0x08      | 1      | uint8_t  | colorMode   | カラーモード                    | 0=モノクロ            |
| 0x09      | 1      | uint8_t  | compression | 圧縮方式                       | 0=非圧縮              |
| 0x0A      | 4      | uint32_t | dataSize    | 画像データサイズ（バイト）       | 計算値                |
| 0x0E      | 8      | uint64_t | md5         | MD5チェックサム（先頭8バイト）   | 任意                  |

### 画像データ

- **配置**: ヘッダーの直後（オフセット22バイト）
- **フォーマット**: 2つのビットプレーン、合計1ピクセルあたり2ビット
- **データサイズの計算式**: `dataSize = ((width * height + 7) / 8) * 2`（バイト単位に切り上げ）
- **格納方法**:
    - 第1ビットプレーン: オフセット22、サイズ `(width * height + 7) / 8` バイト（切り上げ）
    - 第2ビットプレーン: 第1プレーンの直後、同サイズ
    - 各ビットプレーンは**垂直走査順序**（列優先）でピクセルを格納：
        - 列は**右から左**に走査（x = width-1 から 0）
        - 1バイトに**8つの垂直ピクセル**をパック（MSB = グループ内の最上部ピクセル）
    - 完全な仕様については「データ格納の詳細」セクションを参照

### ピクセル値のマッピング

**重要**: Xteink電子ペーパーのLUTは、中間値が入れ替わった非線形グレースケールマッピングを使用します：

| ピクセル値 | バイナリ | Bit1 | Bit2 | LUTレベル | 表示色      |
|-----------|---------|------|------|----------|------------|
| 0         | `00`    | 0    | 0    | L0       | 白         |
| 1         | `01`    | 0    | 1    | L1       | **濃灰**   |
| 2         | `10`    | 1    | 0    | L2       | **淡灰**   |
| 3         | `11`    | 1    | 1    | L3       | 黒         |

**注記**: 値1と値2は、一般的な線形グレースケール順序と比べて入れ替わっています。これはXteinkデバイスの電子ペーパーLUTの動作に合わせたものです。

**ビットプレーンの用途**:

- **Bit1**（第1プレーン）: コマンド `0x24` で送信
- **Bit2**（第2プレーン）: コマンド `0x26` で送信

**ピクセル値の計算式**: `pixelValue = (bit1 << 1) | bit2`

---

## XTCフォーマット（コミックコンテナ）

### ファイル構造

XTCはコミック/PDF閲覧用に、複数のXTGフォーマットページを格納するコンテナフォーマットです。

### ヘッダー（56バイト）

| オフセット | サイズ | 型        | フィールド      | 説明                  | 値                    |
|-----------|--------|----------|----------------|----------------------|----------------------|
| 0x00      | 4      | uint32_t | mark           | ファイル識別子         | 0x00435458 ("XTC\0") |
| 0x04      | 2      | uint16_t | version        | バージョン番号        | 0x0100 (v1.0)        |
| 0x06      | 2      | uint16_t | pageCount      | 総ページ数            | 1-65535              |
| 0x08      | 1      | uint8_t  | readDirection  | 読み方向              | 0-2                  |
| 0x09      | 1      | uint8_t  | hasMetadata    | メタデータの有無       | 0-1                  |
| 0x0A      | 1      | uint8_t  | hasThumbnails  | サムネイルの有無       | 0-1                  |
| 0x0B      | 1      | uint8_t  | hasChapters    | チャプターの有無       | 0-1                  |
| 0x0C      | 4      | uint32_t | currentPage    | 現在のページ（1始まり）| 0-65535              |
| 0x10      | 8      | uint64_t | metadataOffset | メタデータオフセット   | バイトオフセット       |
| 0x18      | 8      | uint64_t | indexOffset    | インデックステーブルオフセット | バイトオフセット |
| 0x20      | 8      | uint64_t | dataOffset     | データ領域オフセット   | バイトオフセット       |
| 0x28      | 8      | uint64_t | thumbOffset    | サムネイルオフセット   | バイトオフセット       |
| 0x30      | 8      | uint64_t | chapterOffset  | チャプターデータオフセット | バイトオフセット   |

### 読み方向

| 値  | 方向    | 説明                           |
|-----|---------|-------------------------------|
| 0   | 左→右   | 左から右（通常）                 |
| 1   | 右→左   | 右から左（日本の漫画）           |
| 2   | 上→下   | 上から下（縦書き）               |

### メタデータ構造（256バイト、任意）

`hasMetadata == 1` の場合、`metadataOffset` の位置に格納：

| オフセット | サイズ | 型        | フィールド    | 説明                                      |
|-----------|--------|----------|--------------|------------------------------------------|
| 0x00      | 128    | char[]   | title        | タイトル（UTF-8、null終端）                 |
| 0x80      | 64     | char[]   | author       | 著者（UTF-8、null終端）                     |
| 0xC0      | 32     | char[]   | publisher    | 出版社/ソース（UTF-8、null終端）             |
| 0xE0      | 16     | char[]   | language     | 言語コード（例: "zh-CN", "en-US"）          |
| 0xF0      | 4      | uint32_t | createTime   | 作成日時（Unixタイムスタンプ）               |
| 0xF4      | 2      | uint16_t | coverPage    | 表紙ページ（0始まり、0xFFFF=なし）           |
| 0xF6      | 2      | uint16_t | chapterCount | チャプター数                                |
| 0xF8      | 8      | uint64_t | reserved     | 予約領域（ゼロ埋め）                         |

### チャプター構造（1チャプターあたり96バイト、任意）

`hasChapters == 1` の場合、`chapterOffset` の位置に格納：

| オフセット | サイズ | 型        | フィールド   | 説明                                |
|-----------|--------|----------|-------------|-------------------------------------|
| 0x00      | 80     | char[]   | chapterName | チャプター名（UTF-8、null終端）       |
| 0x50      | 2      | uint16_t | startPage   | 開始ページ（0始まり）                 |
| 0x52      | 2      | uint16_t | endPage     | 終了ページ（0始まり、含む）           |
| 0x54      | 4      | uint32_t | reserved1   | 予約領域1（ゼロ埋め）                 |
| 0x58      | 4      | uint32_t | reserved2   | 予約領域2（ゼロ埋め）                 |
| 0x5C      | 4      | uint32_t | reserved3   | 予約領域3（ゼロ埋め）                 |

チャプター数は `XtcMetadata.chapterCount` で指定されます。

### ページインデックステーブル（1ページあたり16バイト）

`indexOffset` の位置に格納、ページごとに1エントリ：

| オフセット | サイズ | 型        | フィールド | 説明                                              |
|-----------|--------|----------|-----------|--------------------------------------------------|
| 0x00      | 8      | uint64_t | offset    | XTG/XTH画像のオフセット（絶対値、ファイル先頭から）   |
| 0x08      | 4      | uint32_t | size      | XTG/XTH画像のサイズ（バイト、22バイトのヘッダー含む） |
| 0x0C      | 2      | uint16_t | width     | 画像の幅（ピクセル）                                |
| 0x0E      | 2      | uint16_t | height    | 画像の高さ（ピクセル）                              |

インデックステーブルの合計サイズ: `pageCount * 16` バイト。

### データ領域

すべてのXTG/XTHページ画像は `dataOffset` から格納されます。各ページのデータは連続的に格納され、位置はインデックステーブルの `offset` フィールド（ファイル先頭からの絶対オフセット）で指定されます。

### サムネイル領域（任意）

`hasThumbnails == 1` の場合、サムネイルは `thumbOffset` の位置に格納されます。サムネイルもXTGフォーマットで、同じインデックス構造を使用します。

### ファイルレイアウト

```
[ヘッダー: 56バイト]
[メタデータ: 256バイト] (任意、metadataOffsetの位置)
[チャプター: N × 96バイト] (任意、chapterOffsetの位置)
[ページインデックステーブル: pageCount × 16バイト] (indexOffsetの位置)
[データ領域: 全XTGページデータ] (dataOffsetの位置)
[サムネイル領域] (任意、thumbOffsetの位置)
```

注記: ファイル内のセクションの実際の順序は、ヘッダーのオフセットフィールドによって決定されます。一般的なレイアウトでは、メタデータが最初に配置され、次にチャプター、ページインデックス、ページデータ、最後にサムネイルの順となります。

---

## XTCHフォーマット（コミックコンテナ変種）

### ファイル構造

XTCHはファイル識別子を除き、すべての点でXTCと同一です。

### ヘッダー（56バイト）

XTCと同じ構造で、`mark` フィールドのみ異なります：

| オフセット | サイズ | 型        | フィールド | 説明            | 値                  |
|-----------|--------|----------|-----------|-----------------|---------------------|
| 0x00      | 4      | uint32_t | mark      | ファイル識別子    | 0x48435458 ("XTCH") |

### ファイル識別子の比較

| フォーマット | 識別子（リトルエンディアン） | ASCII   |
|-------------|--------------------------|---------|
| XTC         | 0x00435458               | "XTC\0" |
| XTCH        | 0x48435458               | "XTCH"  |

その他のすべての構造、フィールド、データフォーマットはXTCと同一です。

---

## 文字列エンコーディング

- すべての文字列フィールドは **UTF-8** エンコーディングを使用
- 文字列はnull終端（`\0`）
- 固定サイズの文字列フィールドの残りの領域はゼロ埋め

---

## データ格納の詳細

### ビットマップ格納ルール

#### XTG（1ビット/ピクセル）

- 行: 上から下
- 行内のピクセル: 左から右
- 1バイトあたり8ピクセル
- ビット順序: MSB（ビット7）= 最左ピクセル、LSB（ビット0）= 最右ピクセル

**例**: 幅10ピクセルの画像

- 1行あたり2バイト（切り上げ）
- バイト1: ピクセル0-7（ビット7 = ピクセル0、ビット0 = ピクセル7）
- バイト2: ピクセル8-9（ビット7 = ピクセル8、ビット6 = ピクセル9、未使用ビット = 0）

#### XTH（2ビット/ピクセル）

- 2つのビットプレーンが順次格納
- 第1プレーン: 全ピクセルのBit1（ピクセル値のビット1）、1バイトに8ピクセルをパック
- 第2プレーン: 全ピクセルのBit2（ピクセル値のビット0）、1バイトに8ピクセルをパック
- **重要**: **垂直走査順序**（列優先）を使用、行優先ではない：
    - 列は**右から左**に走査（x = width-1 から 0）
    - 各列内で**8つの垂直ピクセル**が1バイトにパック（上から下）
    - 各バイトは8つの垂直に隣接するピクセルを含む、MSB（ビット7）= グループ内の最上部ピクセル
- この走査順序はXteink電子ペーパーディスプレイのリフレッシュパターンに合致

**ピクセル値の計算式**: `pixelValue = (bit1 << 1) | bit2`

**例**: 480×800ピクセルの画像

- 総ピクセル数: 384000
- 各列: 800ピクセル = 100バイト（800/8 = 100垂直グループ）
- 総列数: 480（右から左に走査）
- 各ビットプレーン: 480 × 100 = 48000バイト
- 第1プレーン: オフセット22、サイズ48000バイト（全ピクセルのBit1を含む）
- 第2プレーン: オフセット48022、サイズ48000バイト（全ピクセルのBit2を含む）
- 合計データサイズ: 96000バイト

---

## 変換ガイドライン

### 他フォーマットからXTG/XTHへの変換

1. **ソース画像の準備**:
    - グレースケールに変換
    - 目標解像度にリサイズ
    - XTGの場合: 閾値処理/二値化を適用
    - XTHの場合: グレースケールを4階調（0-3）にマッピング

2. **ビットマップデータの生成**:
    - XTG: 1ピクセルあたり1ビットでパック、`dataSize = ((width + 7) / 8) * height` で計算
    - XTH: 2つのビットプレーンを生成、`dataSize = ((width * height + 7) / 8) * 2` で計算

3. **ファイルの書き出し**:
    - 正しいファイル識別子でヘッダーを書き出し
    - ビットマップデータを書き出し
    - すべてのマルチバイト値がリトルエンディアンのバイト順であることを確認

### XTC/XTCHから他フォーマットへの変換

1. ヘッダーを読み取り、ページ数とオフセットを取得
2. ページインデックステーブルを読み取り、各ページの位置を特定
3. データ領域から各ページのXTGデータを抽出
4. XTG変換プロセスを使用して各ページを変換

---

## ファイル識別子リファレンス

| フォーマット | リトルエンディアン uint32_t | バイトシーケンス              | ASCII   |
|-------------|--------------------------|----------------------------|---------|
| XTG         | 0x00475458               | `[0x58, 0x54, 0x47, 0x00]` | "XTG\0" |
| XTH         | 0x00485458               | `[0x58, 0x54, 0x48, 0x00]` | "XTH\0" |
| XTC         | 0x00435458               | `[0x58, 0x54, 0x43, 0x00]` | "XTC\0" |
| XTCH        | 0x48435458               | `[0x58, 0x54, 0x43, 0x48]` | "XTCH"  |

---

## 注記

- すべてのオフセットはファイル先頭からのバイトオフセット
- XTC/XTCHのページ番号は表示上は1始まりだが、内部構造では0始まり
- 予約フィールドはゼロ埋めすること
- MD5チェックサムフィールドは任意であり、ゼロでも可
- 圧縮は現在未実装（compressionフィールド = 0）
